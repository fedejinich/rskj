# unitrie-rs-legacy-v1

This directory is an immutable snapshot of the V1 `unitrie-rs` implementation.

## Policy

- This code is frozen and must not be modified.
- Active development happens in `unitrie-rs`.
- Any pull request touching this folder must be rejected by CI.

This snapshot exists to keep a stable performance/parity baseline while `unitrie-rs` evolves.
