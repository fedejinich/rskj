[package]
name = "unitrie-rs-core"
version = "0.1.0"
edition = "2021"
license = "LGPL-3.0-or-later"
description = "Reusable core API for RSK Unitrie without JNI bindings"

[dependencies]
unitrie_rs = { package = "unitrie-rs", path = "../unitrie-rs", default-features = false }
